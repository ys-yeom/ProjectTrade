<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.trade.ProjectTrade.mapper.MemberMapper">
    <select id="selectAll" resultType="memberDTO">
        select * from member
    </select>

    <select id="checkLogin">
        SELECT MEMBER_ID, MEMBER_NAME, EMAIL FROM MEMBER WHERE EMAIL = #{email, jdbcType=VARCHAR} AND PASSWORD = #{password, jdbcType=VARCHAR}
    </select>

    <insert id="insertMember">
        INSERT INTO MEMBER (password, member_name, email, mobile, address) VALUES (#{password}, #{member_name}, #{email}, #{mobile}, #{address})
    </insert>

</mapper>